# Solana Payment Request Creator (Solana Pay)

Создание платёжных запросов в сети Solana через протокол **Solana Pay**. Клиент сканирует QR код или кликает ссылку в Phantom и подтверждает платёж.

## Описание

Этот проект создаёт платёжные запросы через протокол **Solana Pay**. Клиенту не нужно подключать кошелёк на сайте — достаточно отсканировать QR код в мобильном Phantom или кликнуть ссылку в десктопе.

### Как это работает (3 шага):

1. **Вы вводите данные:** адрес получателя (ваш), сумму в SOL, опциональные метку и сообщение
2. **Сервер генерирует:** Solana Pay URL (`solana:<recipient>?amount=...`) и QR код
3. **Клиент подтверждает:** сканирует QR в Phantom mobile или кликает ссылку в десктопе, проверяет детали и подтверждает платёж

Это стандартный протокол Solana Pay — легальный, удобный и безопасный способ принимать платежи. Вы никогда не имеете доступа к приватным ключам клиентов.

## Локальный запуск

```bash
npm install
npm start
```

Откройте `http://localhost:3000`.

## Использование

1. Откройте сайт (локально или публичный URL)
2. Выберите сеть: **testnet** (по умолчанию), devnet или mainnet-beta
3. Введите:
   - **Ваш адрес** (получателя)
   - **Сумму в SOL**
   - Опционально: метку (label) и сообщение (message)
4. Нажмите "Создать платёжный запрос"
5. **Отправьте клиенту:**
   - QR код (для мобильного Phantom) — скриншот или показ на экране
   - Или ссылку (для десктопа) — скопируйте и отправьте
6. Клиент сканирует QR / кликает ссылку → Phantom откроет запрос → клиент подтверждает

## API

### `GET /api/health`

Проверка работы сервера.

**Ответ:**
```json
{
  "ok": true,
  "network": "testnet"
}
```

### `POST /api/create-payment`

Создаёт Solana Pay URL и QR код для платежа.

**Запрос:**
```json
{
  "recipient": "адрес получателя (base58)",
  "amount": число в SOL (например: 0.5),
  "label": "метка (опционально)",
  "message": "сообщение (опционально)",
  "memo": "memo (опционально)",
  "network": "devnet" | "testnet" | "mainnet-beta" (опционально)
}
```



аллалалалалалла
аллалалалалаллааллалалалалаллааллалалалалаллааллалалалалалла
аллалалалалалла
аллалалалалалла
аллалалалалалла
аллалалалалалла
аллалалалалалла

м
м
м

аллалалалалалламм
аллалалалалаллам
аллалалалалалла
аллалалалалалла
м
м
**Ответ:**
```json
{
  "solanaPayUrl": "solana:<recipient>?amount=...",
  "qrCodeDataUrl": "data:image/png;base64,...",
  "recipient": "адрес получателя",
  "amount": число в SOL,
  "amountLamports": число,
  "network": "testnet",
  "rpcUrl": "URL RPC узла",
  "label": "метка или null",
  "message": "сообщение или null",
  "memo": "memo или null"
}
```

## Конфигурация (переменные окружения)

Опционально создайте файл `.env`:

```bash
# Порт (по умолчанию 3000)
PORT=3000

# Сеть (testnet | devnet | mainnet-beta)
# По умолчанию: testnet
SOLANA_CLUSTER=testnet

# Кастомный RPC endpoint (опционально)
# Если не указано, используется публичный clusterApiUrl()
SOLANA_RPC_URL=https://api.testnet.solana.com
```

## Деплой на Vercel

### 🆓 Стоимость: **БЕСПЛАТНО** (Hobby Plan)

Vercel предоставляет бесплатный план навсегда:
- ✅ Неограниченное количество проектов
- ✅ 100GB bandwidth в месяц (более чем достаточно)
- ✅ Автоматические деплои из GitHub
- ✅ SSL сертификаты включены
- ✅ Можно подключить свой домен

**Для вашего проекта этого более чем достаточно!**

### Быстрый деплой:

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Войдите в Vercel:
```bash
vercel login
```

3. Деплойте проект:
```bash
vercel
```

4. Для production деплоя:
```bash
vercel --prod
```

Готово! Vercel даст вам публичный URL вида `https://your-project.vercel.app`.

### Настройка Environment Variables на Vercel:
- Зайдите в Dashboard → ваш проект → Settings → Environment Variables
- Добавьте `SOLANA_CLUSTER=testnet` (или другую сеть)
- Добавьте `SOLANA_RPC_URL` если нужен кастомный RPC

📖 Подробная инструкция: см. [DEPLOY.md](./DEPLOY.md)

### Проблемы с автоматическим деплоем?

Если Vercel не обновляется после изменений на GitHub, см. [VERCEL_TROUBLESHOOTING.md](./VERCEL_TROUBLESHOOTING.md)

## Альтернативные платформы для деплоя

- **Railway**: `railway login` → `railway up`
- **Render**: подключите GitHub репозиторий
- **Heroku**: `heroku create` → `git push heroku main`

## Важно

- По умолчанию используется **testnet** — подходит для тестирования без риска
- Для mainnet-beta переключите сеть на сайте и убедитесь что адрес получателя корректный
- Клиент должен иметь Phantom с достаточным балансом SOL на выбранной сети
- Для тестирования на devnet/testnet можно получить бесплатные SOL:
  ```bash
  solana airdrop 1 <адрес> --url testnet
  ```

## Структура проекта

```
.
├── server/
│   └── index.js          # Express backend
├── public/
│   ├── index.html        # Frontend
│   ├── app.js            # Frontend JS
│   └── styles.css        # Styles
├── package.json
├── vercel.json           # Vercel config
└── README.md
```

## Технологии

- **Backend:** Node.js, Express, @solana/web3.js, qrcode
- **Frontend:** Vanilla JS, HTML, CSS
- **Протокол:** Solana Pay

## Лицензия

MIT
