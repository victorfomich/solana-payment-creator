# Инструкция по деплою на Vercel

## Быстрый старт

### 1. Установите Vercel CLI (если ещё не установлен)

```bash
npm install -g vercel
```

### 2. Войдите в Vercel

```bash
vercel login
```

### 3. Деплойте проект

```bash
vercel
```

Следуйте инструкциям:
- Выберите или создайте проект
- Подтвердите настройки

### 4. Production деплой

```bash
vercel --prod
```

Готово! Вы получите URL вида: `https://your-project.vercel.app`

## Настройка через веб-интерфейс Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New Project"
3. Подключите ваш GitHub репозиторий (или загрузите через CLI)
4. Настройки:
   - **Framework Preset:** Other
   - **Root Directory:** ./
   - **Build Command:** (оставьте пустым)
   - **Output Directory:** (оставьте пустым)
   - **Install Command:** `npm install`

## Environment Variables (переменные окружения)

В настройках проекта на Vercel → Settings → Environment Variables добавьте:

- `SOLANA_CLUSTER` = `testnet` (или `devnet` / `mainnet-beta`)
- `SOLANA_RPC_URL` = (опционально, если нужен кастомный RPC)

## Стоимость Vercel

### 🆓 Hobby Plan (БЕСПЛАТНО)
- **Бесплатно навсегда**
- Неограниченное количество проектов
- 100GB bandwidth в месяц
- Автоматические деплои из GitHub
- SSL сертификаты включены
- Custom domains (можно подключить свой домен)

**Для вашего проекта этого более чем достаточно!**

### 💰 Pro Plan ($20/месяц)
Нужен только если:
- Нужно больше bandwidth (1TB)
- Нужны team features
- Нужны более продвинутые функции

## Проверка работы

После деплоя откройте ваш URL и проверьте:
1. Открывается главная страница
2. Можно ввести адрес и сумму
3. Генерируется QR код и ссылка
4. API работает: `https://your-project.vercel.app/api/health`

## Обновление проекта

После изменений в коде:

```bash
git add .
git commit -m "Update"
git push
```

Vercel автоматически задеплоит новую версию!

Или вручную:

```bash
vercel --prod
```

